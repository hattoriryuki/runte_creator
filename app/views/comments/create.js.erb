$("#error_messages").remove()
<% if @comment.errors.present? %>
  $("#js-comment-form").prepend("<%= j( render 'shared/error_message', object: @comment ) %>");
<% else %>
  $("#js-comments").prepend("<%= j( render 'comments/comment', comment: @comment ) %>");
  $("#js-comment-body").val('');
<% end %>
